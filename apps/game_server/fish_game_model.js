var Stype = require("../Stype");
var Cmd = require("../Cmd");
var Response = require("../Response");

var player_set = {};
var zones = {};

function enter_zone(utag, body, session, proto_type, ret_func){
    
}

module.exports = {
    enter_zone: enter_zone,
}